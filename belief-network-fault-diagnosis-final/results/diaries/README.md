# MATLAB diary logs

The diary files are run logs captured from MATLAB. They show the operational trace of the BFM experiments: model conversion, conditional embedding, evidence creation, solving candidate faults, and exporting results.

They are useful as evidence that the CSV tables were produced by running the BFM scripts rather than by manually entering result values.
