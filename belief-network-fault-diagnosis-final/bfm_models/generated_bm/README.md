# Generated BFM model files

These `.mat` files are MATLAB/BFM model files generated from UIL sources. They are useful for rerunning or inspecting the model inside MATLAB, but they are not the most readable way to understand the model. For explanation, inspect the UIL files and MATLAB export scripts instead.
